# reBotArm 单 Ubuntu 视觉抓取、MuJoCo 仿真与实机安全总体规划

> 文档状态：当前主规划
> 合并来源：`项目规划.md` 的 MuJoCo 落地记录与原“新项目规划”的单 Ubuntu 视觉抓取路线
> 基线日期：2026-08-06
> 当前开发分支：`codex/mujoco-sim-landing`

本文是项目的统一执行依据。`项目规划.md` 保留为 MuJoCo 专项历史记录和实测证据，不再单独决定项目优先级。

---

## 一、项目目标

基于现有 `rebot_Arm` 仓库，完成四条相互衔接但职责隔离的工程链路：

1. 建立 Ubuntu 24.04 + ROS 2 Jazzy 的单机视觉系统；
2. 完成 Gemini 2、YOLO、GraspNet、MoveIt 的完整抓取链路；
3. 重构机械臂连接、使能、失能和执行流程，消除“启动节点即自动上力”；
4. 在保留现有 MuJoCo 成果的基础上补齐、测试和标定数字机械臂环境。

最终形成两个共享 ROS 2 上层接口、但不会同时接管执行的后端：

```text
实机后端：
MoveIt
→ /rebotarm/follow_joint_trajectory
→ rebotarmcontroller
→ 真实机械臂

仿真后端：
MoveIt
→ /rebotarm/follow_joint_trajectory
→ rebotarm_simulation
→ MuJoCo
```

视觉链路统一为：

```text
Gemini 2
→ Ubuntu pyorbbecsdk
→ YOLO
→ GraspNet
→ 抓取候选过滤
→ MoveIt 规划、碰撞检查和执行门控
→ MuJoCo 或真实机械臂
```

项目最终目标不是“仿真能动”或“视觉能出框”，而是形成可复现、可测试、可标定、默认安全的抓取系统。

---

## 二、架构边界

所有新增实现必须遵守 `docs/architecture.md`、`CONTEXT.md` 和 `AGENTS.md`。

| 职责 | 所属包 |
| --- | --- |
| 电机 SDK、通信、enable/disable、最终硬件安全 | `rebotarmcontroller` |
| 轨迹生成、MoveIt adapter、验证、retiming、collision guard | `rebotarm_motion` |
| 视觉、深度、检测、GraspNet 候选和候选质量 | `rebotarm_vision` |
| MuJoCo 模型、物理执行、仿真 ROS adapter 和仿真指标 | `rebotarm_simulation` |
| hand-eye、TCP、TF 和相机标定验证工具 | `rebotarm_calibration` |
| launch 编排和后端选择 | `rebotarm_bringup` |
| 旧导入路径和旧 console script 兼容 | `rebotarm_interactive_control` |

硬规则：

- 不向 `rebotarm_interactive_control` 添加实现逻辑；
- `rebotarm_vision` 不直接调用电机 SDK，也不绕过运动验证；
- `rebotarm_simulation` 不调用真实硬件 SDK；
- `rebotarm_moveit_config` 只保存 URDF、SRDF、规划组、关节限制和碰撞配置；
- 一个功能跨越多个职责时，拆成小模块和明确 ROS 接口，不创建包办全流程的大节点。

---

## 三、当前工程基线

本节区分“代码已存在”“自动测试已验证”和“真实硬件已验收”。三者不能混为一谈。

### 3.1 Ubuntu 视觉环境：代码与环境基础已完成

当前仓库已经具备：

- Ubuntu 24.04 + ROS 2 Jazzy 环境；
- `.venv-vision` 视觉虚拟环境；
- Gemini 2 原生 SDK 安装流程；
- Orbbec udev 规则安装脚本；
- Ubuntu 本地 CUDA YOLO 环境；
- `camera_ubuntu.yaml`；
- `vision_ubuntu.launch.py`；
- ROS 2、PyTorch、OpenCV、Ultralytics、Orbbec SDK 的兼容性处理；
- 本地路径和厂商 SDK 版本管理。

已有记录表明 CUDA、YOLO、ROS 2 和 Orbbec SDK 可以在同一视觉环境导入，但这不等于真实相机数据已经验收。

### 3.2 MuJoCo：基础闭环已经存在

当前分支不是“只有旧版离线轨迹模拟节点”。已经完成：

- 同型号 B601-DM 参考资产接入；
- 独立 MuJoCo Python 环境方案；
- URDF 派生模型和模型 profile；
- visual mesh 与简化 collision geom 初步分离；
- 六轴和夹爪 actuator 初始参数；
- MoveIt → `FollowJointTrajectory` → MuJoCo ROS adapter；
- `/rebotarm/joint_states`、停止和夹爪接口；
- RViz fake sim 与 MuJoCo 物理仿真并存；
- trajectory metrics、summary 和抓取质量字段；
- wall time 与 MuJoCo physics time 同步修复；
- path/goal tolerance 失败记录；
- URDF position limit 与 MuJoCo ctrlrange 的第一层一致性测试；
- step-response suite、viewer 开关和 keyframe fallback 基础设施。

2026-08-06 的当前代码快照中，架构和主要 MuJoCo 单元测试共 44 项通过。该结果证明当前软件基础可继续演进，但不证明动力学已经等同实机。

### 3.3 当前已知的 MuJoCo 风险

- actuator 参数仍是工程初值，不是真实电机、减速器、负载和摩擦的标定结果；
- joint4、joint5、joint6 在部分轨迹中出现明显误差和出力饱和；
- goal/path tolerance 阈值仍需基于 benchmark 调整；
- timeout 尚未完整接入执行循环；
- execute loop 缺少成功、取消、停止、容差失败和超时的完整集成测试；
- grasp scene keyframe 仍可能从不合理的全零伸直姿态开始；
- effort、velocity、acceleration 和 jerk 限制还没有完整跨配置核对；
- collision geom、夹爪接触和抓取质量仍需场景标定；
- viewer 与 ROS executor 长时间并行稳定性尚未验收。

### 3.4 P0 实机安全门状态

P0 修改前，`HardwareManager.connect()` 曾执行：

```text
connect
→ mode_pos_vel
→ enable
→ start_pos_vel_loop
```

该旧流程会让控制节点启动时自动使能机械臂。2026-08-06 的 P0 软件整改已经将连接与使能解耦：连接只建立通信、刷新并校验反馈、确认电机保持失能；只有显式 `/rebotarm/enable` 才能进入当前位置保持。

完整视觉抓取 launch 的当前安全默认值已经改为：

```text
use_hardware:=false
execution_mode:=plan_only
move_to_visual_ready_on_start:=false
shutdown_safe_home:=false
```

软件测试已覆盖连接失能、显式使能、当前位置 hold、反馈/限位检查、失败回滚、动作拒绝和安全 launch 默认值。启动失能 joint states、真实 enable/hold/disable 与故障注入回滚仍需接入机械臂分级验收；完成这些实机证据前，不允许使用完整抓取入口进行真机联调。

### 3.5 当前相机、GraspNet 与标定缺口

- Gemini 2 真实 RGB、深度和稳定性尚未完成硬件验收；
- `Gemini2Driver` 尚未读取 SDK 内参、畸变、深度尺度、实际 stream profile 和设备信息；
- 完整视觉抓取 launch 尚未默认支持 Ubuntu native profile；
- 本地 GraspNet Linux 服务尚未形成正式安装、启动和验收流程；
- hand-eye、TCP 和实际安装 TF 尚未重新标定；
- MuJoCo 几何、动力学和夹爪接触尚未完成面向实机的标定。

---

## 四、总体实施策略

### 4.1 当前仓库作为主仓库

保留现有 Ubuntu 本地化、分层架构和 MuJoCo 实现，不重新 fork，也不以另一个仓库整体覆盖当前工程。

重点保护：

```text
src/rebotarm_simulation/
src/rebotarm_vision/
src/rebotarm_motion/
src/rebotarmcontroller/
tests/test_mujoco_*.py
docs/mujoco_sim.md
docs/mujoco_sim_to_real_params.md
camera_ubuntu.yaml
vision_ubuntu.launch.py
requirements-vision.txt
tools/setup_ubuntu_vision.sh
tools/run_ubuntu_vision.sh
```

### 4.2 上游 MuJoCo 采用 upstream-first 的固定版本迁移

在使用任何“上游实现”前，必须记录：

```text
repository URL
branch
commit SHA
license
目标文件清单
接口差异
测试差异
```

随后建立 current/upstream gap matrix，先把候选上游作为隔离迁移基线运行，再按能力移植缺失模块。P1 已完成差异审核、固定快照、A/B 验证和 source swap；最终由用户决定只使用上游 runtime，切换前实现以 manifest 归档，不作为 active fallback。

当前 P1 主迁移候选为：

```text
repository: https://github.com/huangbinai/robotarm_ros2.git
branch: main
commit: fb28dcdd358b45de79eb47adfb333e2e94e9d5b4
scope: src/rebotarm_simulation/ and its model/config/test/docs files
```

该仓库的 MuJoCo package 已包含 health/headless、viewer、ROS 2 adapter、
URDF-to-MJCF、motor control、collision/contact 和 acceptance 基础设施。它的
`package.xml` 声明 Apache-2.0，但根 `LICENSE` 仍需文件级核验。用户已明确当前
只作本地使用、不发布；因此允许固定版本进入本地 active runtime，但许可证证据
补齐前不得推送或对外再分发上游源码、模型和 mesh。

迁移期间采用双基线策略完成差异验证。P1 收尾后，active `rebotarm_simulation`
只安装上游 runtime；切换前 package-owned 实现保存在
`third_party/rebotarm_simulation_current_baseline`，只用于历史审计和人工回滚。

可能需要评估的上游能力包括：

- 独立 health check；
- 独立原生 viewer；
- 更完整的 URDF-to-MJCF generator；
- 模型、配置和资源安装；
- actuator、sensor、夹爪联动和 contact 配置；
- headless、ROS 2 action 和模型 contract 测试。

上游 runtime 已通过适配进入 active source；必要的 metrics、容差判定和 benchmark
源码可继续保留作离线诊断，但不再暴露 current adapter、legacy CLI 或 backend selector。

### 4.3 仿真与实机严格隔离

任何 MuJoCo 测试必须满足：

```text
use_hardware:=false
不启动 rebotarmcontroller
不打开机械臂串口或 CAN
不调用真实 enable
只有一个 FollowJointTrajectory 服务端
```

仿真后端和实机后端必须通过 launch 参数显式选择。禁止依赖“哪个节点先启动”决定控制权。

### 4.4 分级验收

所有链路按以下层级推进：

```text
静态代码和配置检查
→ unit test
→ headless integration test
→ viewer/RViz 人工观察
→ hardware plan-only
→ hardware low-speed execution
→ end-to-end grasp
```

低层级通过不能自动推导高层级通过。

---

## 五、P0：机械臂安全启动与执行门控

这是所有实机视觉联调的前置条件。

### 5.1 目标状态机

```text
DISCONNECTED
→ CONNECTED_DISABLED
→ ENABLING
→ ENABLED_HOLD
→ TRAJECTORY_RUNNING
→ DISABLING
→ CONNECTED_DISABLED
```

异常路径必须能够进入 `CONNECTED_DISABLED` 或 `DISCONNECTED`，不能留下部分电机使能。

### 5.2 `connect()` 只建立通信

新的 `connect()` 负责：

- 创建 controller；
- 注册电机；
- 建立串口或 CAN 通信；
- 读取全部关节反馈；
- 检查关节数量和在线状态；
- 检查位置、速度和力矩是否为有限值；
- 初始化夹爪通信；
- 保持所有电机失能。

禁止在 `connect()` 内调用：

```text
enable()
start_control_loop()
move_to_home()
move_to_visual_ready()
```

### 5.3 显式 `enable()`

`/rebotarm/enable` 应执行：

1. 获取全部关节最新反馈；
2. 检查通信新鲜度、在线状态和关节数量；
3. 检查当前位置位于软限位内；
4. 先设置 `q_target = q_current`；
5. 逐电机使能并记录已成功的电机；
6. 确认全部电机状态；
7. 启动当前位置保持循环；
8. 切换为 `ENABLED_HOLD`。

任一电机失败时必须停止控制循环、失能所有已使能电机并返回失败。

### 5.4 安全默认参数

完整视觉抓取入口默认改为：

```text
use_hardware:=false
execution_mode:=plan_only
move_to_visual_ready_on_start:=false
shutdown_safe_home:=false
auto_enable:=false
```

实机执行必须显式传入硬件参数，并显式调用 enable。

### 5.5 P0 验收标准

- 启动驱动时机械臂不自动上力；
- 失能状态仍能读取有效 joint states；
- 只有显式 enable 才使能；
- enable 后保持当前位置，不跳向旧目标；
- disable 能停止控制循环并可靠失能；
- 部分使能失败不会留下其他关节使能；
- plan-only 不向实机发送轨迹；
- launch 默认配置不会连接实机。

---

## 六、P1：MuJoCo 基线巩固与差距整合

### 6.1 固定上游来源并制作差距表

先比较当前实现与候选上游，不先复制代码。差距表至少包含：

| 能力 | 当前实现 | `robotarm_ros2` 候选上游 | 采用方案 | 回归测试 |
| --- | --- | --- | --- | --- |
| 模型生成 | 已有 profile/runner | checked-in MJCF、URDF-to-MJCF generator、model contract | 优先比较后选择性本地化 | model/conversion tests |
| ROS adapter | 已有 action/service adapter | 同名 action、gripper、stop 和 `/clock` | 适配接口，保持单 action server | action/launch tests |
| metrics/tolerance | 已有 summary 和 tolerance 失败记录 | 有独立 acceptance/runbook，字段不同 | 保留当前 contract，吸收可复用检查 | metrics/execute tests |
| health check | 部分 headless 测试 | 独立 `mujoco_health` 和 renderer timeout | 本地化 health check，不改变安全默认值 | health test |
| viewer | launch 可选 | 独立 viewer、jog、gravity compensation、POS_VEL | 作为可选仿真入口 | viewer/headless smoke |
| URDF-to-MJCF | 当前为 profile 派生 | 官方 URDF parser + checked-in generated MJCF | 先双生成对照，再决定 generator 归属 | conversion tests |
| 模型资源安装 | 有限 | models/assets、package install 和 venv runbook | 按文件授权和路径审计后本地化 | installed-package test |
| motor/contact | 当前保守 actuator/collision profile | motor control、gripper force、collision baseline | 先导入参数/行为对照，标定后才验收 | motor/contact tests |

### 6.2 先完成执行可靠性技术债

按顺序处理：

1. 固定 `robotarm_ros2` 上游快照，完成 URL/branch/commit、文件清单和 license evidence；
2. 独立运行上游 health/headless/model/ROS/motor/collision 测试，生成差异报告；
3. 选择性本地化模型、URDF-to-MJCF、motor control、viewer 和 health check；
4. 将 timeout 接入 `FollowJointTrajectory` 执行循环；
5. 增加执行循环集成测试，覆盖成功、取消、停止、path tolerance、goal tolerance 和 timeout；
6. 确认 stopped/canceled/tolerance/timeout 不会返回成功；
7. 保持 summary 能记录 stop reason、violated joint 和 threshold；
8. 验证 metrics sampling 不会影响控制循环。

### 6.3 完成关节限制一致性

建立 URDF、MoveIt、MuJoCo 限制对照表：

```text
position
effort / forcerange
velocity
acceleration
jerk
```

验收：

- 同一关节目标在 MoveIt 和 MuJoCo 中含义一致；
- 超限目标能在执行前被测试捕获；
- 错误信息指出关节、限制类型、目标值和允许值。

### 6.4 升级 step-response 与轨迹 benchmark

新增或固化：

- steady-state error；
- settling time；
- overshoot；
- RMS error；
- maximum velocity；
- maximum actuator force；
- force saturation ratio。

历史 current 基线的 joint4、joint5、joint6 跟踪不足已经记录，未被误记为标定成功；
该基线已退出 active runtime。没有真实规格、减速比、电流限制和负载目标时，仍
不得仅通过大幅提高 `forcerange/kp/kv` 宣称参数可迁移实机。

### 6.5 collision、夹爪与抓取质量

后续系统闭环检查（转入 P5/P6，不阻塞 P1 基础仿真收尾）：

- 每个 link 的 collision geom 尺寸；
- 腕部、肘部、底座和夹爪自碰撞；
- finger pad 几何、摩擦和接触参数；
- 空抓、滑落、夹住未抬起和成功抬升；
- 固定物体的 open → pre-grasp → approach → close → lift → retreat → place 序列。

抓取候选归 `rebotarm_vision`，轨迹验证归 `rebotarm_motion`，物理执行和接触反馈归 `rebotarm_simulation`。

### 6.6 MuJoCo 基础验收

最低验收层次：

1. 模型可从当前 URDF 或明确的派生配置生成；
2. 模型、mesh、关节和 actuator 数量符合 contract；
3. headless 物理步进有限且无 NaN；
4. viewer 中关节方向、home 姿态和夹爪开合合理；
5. `/joint_states`、`/clock`、轨迹 action 和夹爪接口正常；
6. MoveIt 能规划并驱动 MuJoCo；
7. 整个过程不启动实机节点；
8. 失败轨迹返回失败并留下可解释指标。

---

## 七、P2：Gemini 2 SDK 与真实 RGB-D 验收

### 7.1 增加相机标定接口

`Gemini2Driver` 应从 Orbbec SDK 获取并提供：

```text
设备名称、序列号、固件版本、USB 类型
实际 RGB stream profile
实际 depth stream profile
RGB 和 depth intrinsics
distortion coefficients
RGB-depth extrinsics
depth scale
align mode
```

禁止根据图像尺寸估算内参。请求 profile 失败并回落默认 profile 时必须显式记录实际配置。

### 7.2 深度单位约定

SDK 原始深度先按真实 `depth_scale` 转换。

ROS 输出约定：

```text
/camera/color/image_raw
/camera/color/camera_info
/camera/depth/image_raw
/camera/depth/camera_info
```

- 若输出 `16UC1`，明确统一为毫米；
- 点云和抓取算法内部使用 `float32` 米；
- `CameraInfo` 必须对应实际分辨率和对齐后的坐标系。

### 7.3 P2 验收标准

- `lsusb` 和 SDK 都识别同一设备；
- RGB、深度长时间稳定输出；
- 实际 profile 与日志一致；
- CameraInfo 来源于 SDK；
- 深度中心点和多个距离样本误差可记录；
- RGB-D 对齐后对应同一像素坐标系；
- 分辨率变化时内参能同步更新或明确拒绝不支持配置。

---

## 八、P3：完整单 Ubuntu 视觉链路

### 8.1 增加显式视觉 profile

完整 bringup 支持：

```text
vision_profile:=ubuntu_native
vision_profile:=network
```

映射：

```text
ubuntu_native
→ camera_ubuntu.yaml
→ Gemini 2 本地 SDK
→ 本地 YOLO 模型
→ yolo_device=0

network
→ camera.yaml
→ 明确的网络相机和检测服务
```

不能以 localhost 作为“无 Windows 依赖”的充分证据；必须确认提供该端口的进程运行在当前 Ubuntu。

### 8.2 安全统一入口

开发默认启动示例：

```bash
ros2 launch rebotarm_bringup visual_grasp_system.launch.py \
  vision_profile:=ubuntu_native \
  use_hardware:=false \
  execution_mode:=plan_only \
  move_to_visual_ready_on_start:=false
```

### 8.3 P3 验收标准

一次 launch 后至少具备：

```text
/camera/color/image_raw
/camera/color/camera_info
/camera/depth/image_raw
/camera/depth/camera_info
/grasp/detections
/camera/color/annotated
```

同时满足：

- 不依赖 Windows 路径或 Windows 服务；
- 默认不连接实机；
- 时间戳、frame_id 和 TF 一致；
- 视觉失效时不会复用过期候选执行动作。

---

## 九、P4：Ubuntu 本地 GraspNet

### 9.1 第一阶段采用双环境单系统

```text
.venv-vision
  ROS 2 Jazzy
  Gemini 2
  YOLO
  OpenCV

.venv-graspnet
  GraspNet
  PyTorch
  CUDA extensions
  PointNet2/KNN
```

第一阶段不强行把旧 GraspNet 依赖安装进 ROS 2 Python 3.12 环境。

### 9.2 第一阶段通信协议

可以暂时保留：

```text
http://127.0.0.1:8081/graspnet_candidates.json
```

但需要新增并验收 Linux 本地服务：

```text
tools/setup_ubuntu_graspnet.sh
tools/run_ubuntu_graspnet.sh
requirements-graspnet.txt
docs/ubuntu_graspnet_setup_zh.md
```

### 9.3 数据 contract

输入：

```text
RGB
depth in meters
camera intrinsics
YOLO bounding box
target label
timestamp
frame_id
```

输出：

```text
position
rotation matrix or normalized quaternion
gripper width
confidence
source timestamp
camera_depth_frame
model/checkpoint identity
```

### 9.4 P4 验收标准

- Ubuntu 不启动 Windows 服务；
- localhost 服务能独立启动、停止和报告健康状态；
- `/grasp/graspnet_candidates` 产生有效候选；
- 候选时间戳和 frame_id 正确；
- 同一静态目标候选不会大幅跳变；
- 无候选、过期候选或 TF 失败时系统停止；
- 不自动回退到未经同等验证的简化抓取。

本地服务稳定后，再评估 `source_mode: local_backend`，不把依赖整合作为第一阶段前置条件。

---

## 十、P5：标定

标定在基础链路通过后进行，不与大规模代码移植混在同一提交中。

### 10.1 相机内参与深度验证

- 优先采用 SDK 原始参数；
- 验证深度尺度、畸变和 RGB-D 对齐；
- 验证不同 profile 下内参更新；
- 记录测量方法、样本距离和误差，而不是只写“看起来正确”。

### 10.2 hand-eye 标定

已有 `end_link → camera_depth_frame` 只能视为历史配置，不能视为当前安装结果。

执行：

```text
末端多姿态采样
→ 标定板检测
→ AX=XB 求解
→ 异常样本剔除
→ 三维点和 TF 验证
```

验收：同一静态物体转换到 `base_link` 后，在机械臂不同姿态下位置保持稳定。

### 10.3 TCP 标定

标定 `end_link → grasp_tcp`，明确：

- 夹爪中心；
- 闭合中心线；
- 指尖接触位置；
- 抓取宽度定义；
- approach direction。

### 10.4 MuJoCo 几何标定

检查关节轴方向、link 长度、mesh 原点、关节零位、夹爪范围、桌面高度和相机安装位姿。

### 10.5 MuJoCo 动力学标定

按以下顺序推进：

1. 几何一致；
2. 关节方向和零位一致；
3. 位置响应一致；
4. 速度响应一致；
5. 夹爪接触一致；
6. 重力负载趋势一致；
7. 再调整质量、质心、惯量、阻尼、摩擦、增益和力矩限制。

所有调参必须保存 benchmark 输入、参数版本和输出指标。仿真演示参数与实机映射参数分开管理。

---

## 十一、P6：系统集成与最终验收

### 11.1 真实视觉 + MuJoCo 执行

第一阶段使用真实 Gemini 2 感知，MuJoCo 执行：

```text
Gemini 2
→ YOLO
→ GraspNet
→ TF
→ IK/MoveIt
→ MuJoCo
```

用于验证候选生成、TF、IK、轨迹规划、抓取状态机和仿真执行接口。

### 11.2 完全离线虚拟 RGB-D

第二阶段增加：

```text
MuJoCo RGB camera
MuJoCo depth camera
CameraInfo
virtual object labels
```

最终形成：

```text
MuJoCo RGB-D
→ YOLO/GraspNet
→ MoveIt
→ MuJoCo
```

### 11.3 实机 plan-only

```text
use_hardware:=true
execution_mode:=plan_only
auto_enable:=false
move_to_visual_ready_on_start:=false
```

验收：驱动连接但保持失能，视觉和 MoveIt 可运行，不发送真实轨迹。

### 11.4 实机低速执行

显式 enable 后，依次只执行：

1. 单关节小角度；
2. 单个安全姿态；
3. pre-grasp；
4. approach；
5. close gripper；
6. lift；
7. retreat 和 disable。

每一级通过并记录结果后才能进入下一级。

### 11.5 完整自动抓取

最终验收要求：

- 默认启动安全；
- 相机、检测、候选、TF、IK 和轨迹均有新鲜度检查；
- collision 和 runtime guard 能阻止危险执行；
- 无候选、规划失败、容差失败、超时和通信异常都进入安全停止；
- 仿真和实机共享上层接口，但安全阈值和执行后端明确分离；
- 抓取结果能够区分空抓、滑落、未抬起和成功。

---

## 十二、分支与提交组织

建议使用独立工作分支：

```text
codex/hardware-safe-enable
codex/mujoco-gap-integration
codex/gemini2-camera-info
codex/ubuntu-visual-profile
codex/ubuntu-graspnet
codex/calibration
codex/end-to-end-grasp
```

每个分支只处理一个主要职责。上游移植、硬件安全、相机、GraspNet 和标定不能混成一个难以回滚的提交。

建议提交顺序：

```text
1. safety: separate hardware connection from motor enable
2. safety: default visual grasp launch to plan-only
3. test: cover enable rollback and safe launch defaults
4. docs: pin upstream mujoco source and record gap matrix
5. fix: complete mujoco timeout and execute-loop failure handling
6. test: extend mujoco limits, benchmark and interface acceptance
7. feat: selectively integrate missing upstream mujoco capabilities
8. feat: publish Gemini 2 calibrated camera info and actual profiles
9. feat: add ubuntu native visual grasp profile
10. feat: add local ubuntu graspnet service
11. calibration: update camera, hand-eye and TCP transforms
12. calibration: tune mujoco geometry and measured motor response
13. test: add end-to-end simulation and low-speed hardware acceptance
14. docs: record verified final baseline
```

---

## 十三、每阶段必做检查

涉及包归属或新增模块时：

```bash
python3 -m pytest tests/test_package_layering.py -q
python3 -m pytest tests -q
python3 -m compileall \
  src/rebotarm_dashboard/rebotarm_dashboard \
  src/rebotarm_teleop/rebotarm_teleop \
  src/rebotarm_teach/rebotarm_teach \
  src/rebotarm_motion/rebotarm_motion \
  src/rebotarm_interactive_control/rebotarm_interactive_control -q
```

修改 launch 时额外执行：

```bash
python3 -m compileall src/rebotarm_bringup/launch -q
```

MuJoCo 变更至少执行对应 model、headless、adapter、metrics、limit 和 launch 测试。硬件相关代码通过自动测试后，仍需按 plan-only 和低速分级验收，不能由 mock 测试代替实机验证。

---

## 十四、当前最合适的下一步

P1 已完成，当前执行顺序更新为：

```text
1. P0 connect/enable 安全门保持已验收状态
2. P1 upstream-only MuJoCo 本地基线保持冻结，不发布未获完整授权的上游资产
3. P2 验收真实 Gemini 2 RGB、depth、设备信息和实际 stream profiles
4. P2 读取 intrinsics、distortion、extrinsics 与 depth scale
5. P2 验证 RGB-D 对齐、CameraInfo 和已知距离深度误差
6. P3 接入 Ubuntu native 完整视觉链路
7. P4 建立 Ubuntu 本地 GraspNet 服务
8. P5/P6 完成 hand-eye/TCP/MuJoCo 标定和自动抓取闭环
```

P2 只授权相机枚举和 RGB-D 采集，不自动授权机械臂运动；所有实机执行仍受 P0 安全门和 P5/P6 分级验收约束。
