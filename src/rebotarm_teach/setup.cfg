[develop]
# 开发期（colcon build 的默认符号链接安装）控制台脚本落地目录；
# $base 是安装前缀，脚本实际写在 lib/rebotarm_teach 下，由 ROS 2 负责加入 PATH。
script_dir=$base/lib/rebotarm_teach
[install]
# 正式安装（--merge-install / 系统安装）时控制台脚本的落地目录；与 develop 段保持一致。
install_scripts=$base/lib/rebotarm_teach
