"""面板包的包标识模块（仅承载包级说明，不导出任何符号）。

职责
    Web 应用边界层：面板首页与静态资源、本机 HTTP 路由、SSE 状态流、面板状态聚合、
    调用遥操作/示教/控制器的服务与动作，以及面板专用的模型文件与网格转发。

边界
    只做展示与指令转发，不含复杂运动规划、轨迹重定时、示教回放算法与硬件 SDK 代码；
    运动与示教的算法结果由面板呈现，不在本包内重新实现。HTTP 仅面向本机界面/接口。

使用方式
    导入本包不产生副作用；可执行入口在 ``setup.py`` 的 ``console_scripts`` 中声明，
    节点参数由启动组合层的参数文件提供。
"""
