# ament_python 安装布局：把 console_scripts 生成的可执行文件放到 lib/rebotarm_teleop，
# 这样 `ros2 run rebotarm_teleop <可执行名>` 与启动文件里的 executable= 才能解析到；
# $base 是安装前缀（工作空间的 install/<包名>），两节必须保持一致，改名会破坏入口发现。
[develop]
script_dir=$base/lib/rebotarm_teleop
[install]
install_scripts=$base/lib/rebotarm_teleop
