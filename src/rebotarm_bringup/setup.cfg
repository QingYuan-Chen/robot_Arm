# setuptools 配置：约定可执行脚本的安装位置。
#
# 本包没有 console_scripts 入口，因此这里的两个目录只需存在即可；
# 它们保证 ament_python 包的标准布局被满足，ros2 才能在环境中正确识别本包。

[develop]
# 开发模式（可编辑安装）下，脚本目录指向本包在 lib 下的路径
script_dir=$base/lib/rebotarm_bringup
[install]
# 正式安装时，脚本同样落到 lib 下的包专用目录
install_scripts=$base/lib/rebotarm_bringup
