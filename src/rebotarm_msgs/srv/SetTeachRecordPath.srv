# 设置示教录制文件路径的服务。由示教包的录制节点提供，服务名形如
# /<命名空间>/teleop/teach_record/set_path（依次为 命名空间、遥操作、示教录制、动作名）；
# 界面通过它指定本次录制保存到哪个文件。

# ── 请求 ──────────────────────────────────────────────────────────
# 录制目标路径（字符串）。允许只给文件名，节点会把它拼到默认录制目录
# teleop_records/ 之下；也接受绝对路径，由节点做归一化与安全检查。
string record_path
---
# ── 响应 ──────────────────────────────────────────────────────────
# 路径是否被接受并生效。
bool success
# 说明文本（成功摘要或拒绝原因，英文）。
string message
# 归一化后真正使用的路径，供界面回显确认；失败时为空串或原值，取决于节点实现。
string normalized_path
