# 关节零点标定服务：把"当前位置"记为编码器零点，只改坐标不产生运动。
# 由硬件包提供，服务名 /<namespace>/set_zero；要求硬件处于已连接但未使能状态
# （硬件层会先退出手动拖拽/重力补偿模式）。

# ── 请求 ──────────────────────────────────────────────────────────
# 要标定的关节名。空串表示全部关节一起回零；填 "gripper" 时单独标定夹爪；
# 其它取值应为硬件 joint_names 中的关节名。
string joint_name
---
# ── 响应 ──────────────────────────────────────────────────────────
# 标定是否成功。
bool success
# 说明文本："set_zero complete" / "set_zero failed" 或异常文本（英文）。
string message
