# 电机控制模式切换服务。由硬件包提供，服务名 /<namespace>/set_mode。
# 切换会改变电机侧控制律，属于慢操作（与使能、回零互斥），且会先退出手动拖拽模式。

# ── 请求 ──────────────────────────────────────────────────────────
# 目标模式，字符串（硬件层负责小写化与合法性校验），取值："mit"、"pos_vel"、"vel"。
string mode
---
# ── 响应 ──────────────────────────────────────────────────────────
# 模式是否切换完成。
bool success
# 说明文本：成功为 "mode set to <mode>"，失败为 "mode switch incomplete" 或异常文本。
string message
