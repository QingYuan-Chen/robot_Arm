# 夹爪力闭合抓取服务：闭合到"堵转"（推断为接触）后进入有界保持。
# 由硬件包提供，服务名 /<namespace>/gripper/grasp。
# 重要：该夹爪没有力传感器，"接触"是由闭合行程 + 速度堵转推断出来的，不是实测接触力；
# 字段名虽含 force，实际物理量是夹爪电机力矩，单位 N·m。

# ── 请求 ──────────────────────────────────────────────────────────
# 闭合阶段力矩，单位 N·m；硬件层夹到 [0.05, 1.0]。它决定夹爪被推进多深才堵转，
# 因此实际上是"夹持力旋钮"（越大夹得越紧，也越容易顶坏目标）。
float64 close_force
# 保持阶段前馈力矩，单位 N·m；硬件层夹到 [0.05, 1.5]。
# 注意它只是叠加在位置项上的前馈，不应作为夹持力旋钮使用。
float64 hold_force
# 闭合超时，单位 s（硬件层下限 0.1）；超时仍未检测到堵转即判失败。
float64 close_timeout_sec
# 最短闭合时间，单位 s；防止刚起步速度尚未建立就被误判为接触。
float64 min_close_time_sec
# 判定"已停住"的速度阈值，单位 rad/s；夹爪电机角速度绝对值需低于它。
float64 velocity_threshold
# 判定接触所需的最小闭合行程，单位 m；行程不足说明没夹到东西。
float64 min_closure_distance_m
---
# ── 响应 ──────────────────────────────────────────────────────────
# 闭合流程是否成功走完。当前实现里只有检测到堵转才返回 true，因此它与
# contact_detected 同真同假：闭合超时未堵转 = false（夹空/未夹到）。
bool success
# 是否检测到接触（堵转）。注意这是"闭合行程 + 速度堵转"推断出的接触，
# 不是实测接触力。
bool contact_detected
# 检测到接触时的夹爪开口位置，单位 m；未检测到接触时为 0.0。
float64 contact_position
# 流程结束时的实际开口位置，单位 m；失败时也回填当前值。
float64 reached_position
# 实际生效的保持力矩，单位 N·m。
float64 hold_force
# 硬件层说明文本（含保持时长上限等信息，英文）。
string message
