# 位姿执行服务：规划（可选执行）一个末端位姿目标，是"规划 + 下发轨迹"的标准入口。
# 由运动执行节点提供，服务名 /<namespace>/motion_execution/execute_pose。

# ── 请求 ──────────────────────────────────────────────────────────
# 目标位姿。header.frame_id 给定位姿所在坐标系（默认 base_link）；
# position 单位 m，orientation 为四元数。
geometry_msgs/PoseStamped target_pose
# 速度缩放系数，无量纲，取值 (0, 1]；越小越慢越安全。
# 传 0.0 表示"未指定"，节点会回落到参数 default_velocity_scaling（默认 0.10）。
float64 velocity_scaling
# 加速度缩放系数，无量纲，取值 (0, 1]；传 0.0 时回落到 default_acceleration_scaling（默认 0.08）。
float64 acceleration_scaling
# 单步超时，单位 s。实际使用时会被下限保护（等价于 max(timeout_sec, 1.0)），
# 等待执行结果时另有至少 30 s 的窗口；传 0 不会立即判失败。
float64 timeout_sec
# 为 false 时只规划、不下发轨迹（干跑，用于审查可达性）；为 true 才真正执行。
bool execute
# 仅供 execute=false 的连续纯规划预览使用；空 name 表示从 MoveIt 当前状态规划。
# execute=true 且非空时服务端必须拒绝，不能拿虚拟起点驱动真机。
sensor_msgs/JointState preview_start_joint_state
# 为 true 时只返回规划轨迹，不立即向 RViz 发布。多阶段调用方可收集所有结果后，
# 再通过 PublishTrajectoryPreview 一次发布，避免 RViz 在阶段边界反复重启动画。
# 该字段不影响 execute=true 的真实执行路径。
bool suppress_preview
---
# ── 响应 ──────────────────────────────────────────────────────────
# 本次请求是否成功。规划失败、动作服务器不可用、目标被拒或执行超时都为 false。
bool success
# 失败/成功发生在哪个阶段，字符串枚举："planning"（规划阶段）或 "execution"（下发执行阶段）。
string stage
# 说明文本：成功时为规划器消息；失败时为失败原因（英文）。
string message
# 规划出的关节轨迹。只要规划成功就会回填（即使 execute 为 false），便于调用方审查。
trajectory_msgs/JointTrajectory planned_trajectory
