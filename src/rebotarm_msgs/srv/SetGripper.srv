# 夹爪位置控制服务：张开/闭合到指定开口距离并等待到位。
# 由硬件包提供，服务名 /<namespace>/gripper/set；
# 仿真侧的同名服务用于让仿真链路复用同一套调用约定。

# ── 请求 ──────────────────────────────────────────────────────────
# 目标开口距离，单位 m：0 = 完全闭合，正值 = 张开宽度（不是电机角度）。
# 硬件层会夹到可信行程内（本站约 [0, 0.085]），超范围不会报错而是被裁剪。
float64 position
# 最大力矩，单位 N·m；传 <= 0（默认 0.0）表示使用硬件层默认力矩。
float64 max_effort
---
# ── 响应 ──────────────────────────────────────────────────────────
# 是否在超时内到达目标位置。
bool success
# 实际到达的开口距离，单位 m；失败时回填当前值（可能远离目标）。
float64 reached_position
