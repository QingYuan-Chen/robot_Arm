# ── Goal：相对当前位姿的直线偏移目标 ──────────────────────────────
# 运动轴，字符串枚举："x"、"y"、"z"（其它取值会被服务端拒绝）。
string axis
# 沿该轴的位移量，单位 m，带符号（正负表示轴的正/负方向）。
# 安全上限由服务端校验（仿真通道限制 |distance_m| <= 0.05）。
float64 distance_m
# 参考坐标系名，默认 "base_link"（服务端在空串时回落到该值）。
string frame_id
# 速度缩放系数，无量纲，取值范围 [0.05, 1.0]；越小越慢，越界会被拒绝。
float64 speed_scale
---
# ── Result：本次目标的终局结果 ────────────────────────────────────
# 是否成功完成（规划并下发执行）。校验失败、执行失败或异常均为 false。
bool success
# 结果说明（英文短句）：成功时为执行摘要，失败时为拒绝/失败原因。
string message
---
# ── Feedback：执行阶段的粗粒度进度 ───────────────────────────────
# 阶段名，字符串枚举，顺序为："planning"（规划中）、"dispatching"（下发轨迹）、
# "executing"（执行中）。
string state
# 进度，无量纲，[0, 1]；当前实现按阶段给出固定值：planning 0.2、dispatching 0.6、
# executing 0.9，不随实际轨迹连续变化。
float64 progress
