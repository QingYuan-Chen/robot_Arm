"""运动包的包标识模块（仅承载包级说明，不导出任何符号）。

职责
    运动链路中"生成与校验"这一层：点到点预览与执行节点、规划客户端适配、位姿预览/逆解预览、
    轨迹重定时、速度/加速度/加加速度校验、回放运行时跟踪守卫、碰撞预检、示教回放起始对齐，
    以及关节轨迹构造工具。

边界
    本包可以调用规划服务与控制器动作，但**不得直接访问电机 SDK**——硬件访问统一收敛在底层
    硬件控制包里；示教录制/回放编排属于示教包，键盘/Web/RViz 操作适配属于操作适配包。

对外接口
    可执行入口在 ``setup.py`` 的 ``console_scripts`` 中声明；节点参数由 ``config/`` 下的
    参数文件提供（例如视觉就绪位姿）。导入本包不会产生副作用。
"""
