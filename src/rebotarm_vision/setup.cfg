# ament_python 的脚本安装位置配置。
# 本包的可执行节点由 setup.py 的 console_scripts 生成，ros2 run 依赖该文件把脚本装到
# lib/<包名> 下，因此这里的路径必须与包名 rebotarm_vision 一致，不能改动。
[develop]
script_dir=$base/lib/rebotarm_vision
[install]
install_scripts=$base/lib/rebotarm_vision
