"""视觉包：reBotArm 的 Gemini2 相机取流、目标检测、抓取候选生成与视觉抓取集成。

在系统中的位置：位于感知层，上游是相机/深度设备与检测模型，下游是运动层与控制器。
包内只做"感知 + 抓取候选决策 + 把计划翻译成受门控的动作序列"，所有真实运动都必须
经由运动/控制器提供的服务完成。

子模块划分：
- ``camera``：相机驱动（协议与 Gemini2 实现）、取流与预热；
- ``detector``：检测器封装（目标框/掩膜）；
- ``converters``：内部结果与 ROS 消息之间的转换；
- ``utils``：可视化等辅助函数；
- 根目录下的各 ``*_policy`` 模块：以纯函数/不可变配置的形式承载候选过滤、评分、
  位姿策略、夹爪策略、安全撤退等决策，便于单测且不接触硬件。

安全约束：本包不得绕过运动校验或控制器保护；任何抓取动作都必须经过规划、
碰撞检查与执行门控，参数默认值以"不产生意外运动"为准。
"""
