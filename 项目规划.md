# reBotArm 项目规划

本文记录当前 reBotArm 工程的 MuJoCo / MoveIt 仿真落地进度、已完成部分审核结论，以及后续为了接近真实项目可用状态需要继续优化的事项。

## 当前目标

本阶段目标不是替代真实硬件控制器，而是建立一条稳定的离线物理仿真链路：

```text
MoveIt / RViz 规划
  -> FollowJointTrajectory
  -> MuJoCo ROS adapter
  -> MuJoCo 物理执行
  -> joint_states / gripper state 回传
```

工程边界保持不变：

- `rebotarm_simulation` 负责 MuJoCo 仿真、离线验证、仿真 ROS adapter。
- `rebotarm_moveit_config` 负责 URDF、SRDF、规划组、关节限制和 RViz 配置。
- `rebotarmcontroller` 仍然是未来真实硬件通信和最后安全边界。
- 不把新实现逻辑放入 `rebotarm_interactive_control`。

## 已完成阶段

### 阶段 1：同型号 MuJoCo 资产接入

已使用同型号 B601-DM 机械臂的 MuJoCo 参考仓库作为第三方资产来源：

```text
third_party/reBotArm_develop_hjx
```

该目录作为参考资产使用，不直接破坏或改写原项目结构。

### 阶段 2：MuJoCo 运行环境

已采用独立虚拟环境管理 MuJoCo 依赖：

```text
third_party/rebotarm_mujoco_venv
requirements-mujoco.txt
```

ROS 2 Jazzy 仍使用系统 Python，MuJoCo 使用 `venv --system-site-packages`，避免 Conda 和 ROS 2 的 `rclpy`、Qt、OpenCV、OpenSSL、`libstdc++` 依赖发生冲突。

### 阶段 3：物理模型生成工具

已在 `rebotarm_simulation` 内新增 MuJoCo 模型生成和验证工具：

```text
src/rebotarm_simulation/rebotarm_simulation/mujoco_model_profile.py
src/rebotarm_simulation/rebotarm_simulation/mujoco_runner.py
src/rebotarm_simulation/rebotarm_simulation/mujoco_cli.py
```

生成结果位于：

```text
build/mujoco_models/
```

这些是可重复生成文件，不作为主工程源码提交。

### 阶段 4：visual / collision 初步分离

已把原 STL mesh 作为 visual-only 使用，并为主要连杆、底座、夹爪指尖新增简化 collision geom。

当前策略：

- mesh visual：`contype=0`、`conaffinity=0`、`group=1`
- collision geom：box / cylinder 简化几何，`group=4`
- 夹爪左右 finger pad 已处理自碰撞排除，避免夹爪闭合被自己挡住。

### 阶段 5：电机参数初版

已为六轴和夹爪配置初始 actuator 参数：

```text
joint1..joint3: forcerange -27 27, kp 270
joint4..joint6: forcerange -7 7,   kp 70
gripper:        forcerange -20 20, kp 600, kv 60
```

该配置能支持当前基础轨迹执行和夹爪闭合验证，但仍属于初版仿真参数，不等同于真实电机标定参数。

### 阶段 6：MoveIt 到 MuJoCo 执行链路

已新增独立 MuJoCo ROS adapter：

```text
src/rebotarm_simulation/rebotarm_simulation/mujoco_adapter_core.py
src/rebotarm_simulation/rebotarm_simulation/mujoco_ros_adapter_node.py
src/rebotarm_simulation/rebotarm_simulation/mujoco_metrics.py
```

ROS 接口：

```text
/rebotarm/follow_joint_trajectory
/rebotarm/joint_states
/rebotarm/trajectory_stop
/rebotarm/gripper/set
/rebotarm/gripper/state
```

### 阶段 7：保留原轻量化仿真

已新增 MuJoCo + MoveIt launch，但不替换原来的 RViz fake sim：

```text
src/rebotarm_simulation/launch/mujoco_moveit_sim.launch.py
```

启动命令：

```bash
cd /home/a/project/rebot_Arm
source /opt/ros/jazzy/setup.bash
source install/setup.bash

ros2 launch rebotarm_simulation mujoco_moveit_sim.launch.py \
  use_rviz:=true \
  python_executable:=third_party/rebotarm_mujoco_venv/bin/python
```

### 阶段 8：执行误差记录

每次执行轨迹后会输出：

```text
build/mujoco_runs/latest/trajectory_metrics.csv
build/mujoco_runs/latest/summary.json
```

这些文件用于判断：

- MoveIt 是否成功下发轨迹；
- MuJoCo 是否实际跟上轨迹；
- 最大误差、RMS 误差、最大速度、最大 actuator force 是否异常。

## 已完成部分审核

### 审核结论

当前已完成部分可以作为“MoveIt + MuJoCo 基础闭环”的第一版使用，但还不能直接宣称达到真实硬件落地精度。

通过项：

- 新代码放在 `rebotarm_simulation`，没有把仿真实现放进 `rebotarm_interactive_control`。
- MuJoCo adapter 没有直接调用真实硬件 SDK，符合硬件边界。
- 原 RViz fake sim 没有被替换，仍可作为轻量化演示路径保留。
- MoveIt 轨迹可以进入 MuJoCo 物理步进。
- 夹爪控制已经从纯 RViz 显示提升为 MuJoCo actuator 控制。
- 轨迹执行后有 metrics 输出，便于后续调参。

保留风险：

- 当前 actuator 参数是工程初值，不是基于真实电机、减速器、负载和摩擦标定得到。
- MuJoCo、URDF、MoveIt 三处关节限制仍需要做一次系统一致性审核。
- collision geom 是初版简化模型，需要继续用接触场景验证误碰撞和漏碰撞。
- `FollowJointTrajectory` 目前主要关注执行流程和误差记录，后续还要加强 goal tolerance / path tolerance 的失败判定。
- 当前 MuJoCo viewer 没有和 launch 集成，默认只在 RViz 观察 ROS 状态。
- 自动抓取序列还没有纳入 MuJoCo 闭环，只完成了单条轨迹和夹爪服务。

## 后续优化路线

### 第一优先级：关节限制一致性

目标：让 URDF、SRDF、MoveIt joint_limits、MuJoCo ctrlrange 保持一致。

操作：

1. 列出 `joint1..joint6`、左右夹爪的全部限制来源。
2. 建立一份限制对照表。
3. 用测试检查 MoveIt 和 MuJoCo 的限制是否漂移。
4. 对超限目标给出明确失败原因，而不是只表现为“机械臂不动”。

验收：

- 同一组关节目标在 MoveIt 和 MuJoCo 中含义一致。
- 超限目标能被测试捕获。

### 第二优先级：轨迹跟踪参数标定

目标：降低 MuJoCo 执行误差，让仿真更接近真实执行。

操作：

1. 对每个关节做 step-response。
2. 记录上升时间、超调、稳态误差和最大 actuator force。
3. 调整 `kp`、`kv`、`damping`、`frictionloss`、`armature`。
4. 给重载姿态和高速轨迹建立 benchmark。

验收：

- `summary.json` 中 RMS error 稳定下降。
- actuator force 不长期顶到 `forcerange`。
- 轨迹不会靠抖动或穿透完成。

### 第三优先级：collision 模型校准

目标：让碰撞既足够快，又不明显失真。

操作：

1. 对每个 link 的 collision geom 做尺寸复核。
2. 检查夹爪、腕部、肘部、底座附近的自碰撞。
3. 建立典型姿态碰撞测试。
4. 为抓取物体接触单独调整 finger pad 几何和摩擦。

验收：

- 常见有效姿态不被误判自碰撞。
- 明显穿模姿态能被检测出来。
- 夹爪接触稳定，不因自碰撞无法闭合。

### 第四优先级：轨迹执行失败判定

目标：让“物理做不到”时有明确反馈。

操作：

1. 在 adapter 中加入 goal tolerance / path tolerance 判断。
2. 当误差超过阈值时返回失败，而不是只写 metrics。
3. 将停止、取消、超时、误差过大分别记录到 `summary.json`。
4. 在 RViz / MoveIt 执行失败时输出可读原因。

验收：

- 轨迹跟踪失败能明确返回失败状态。
- `summary.json` 能解释失败原因。

### 第五优先级：夹爪接触和抓取质量

目标：从“夹爪能开合”提升到“能判断是否抓住”。

操作：

1. 增加夹爪接触检测。
2. 记录闭合距离、接触力、物体位移。
3. 判断空抓、滑落、夹住但未抬起等状态。
4. 和 `rebotarm_vision` 的抓取候选质量评估对接。

验收：

- 夹爪闭合后能区分抓住和没抓住。
- 抬起阶段能判断物体是否跟随。

### 第六优先级：自动抓取序列

目标：实现可复用的仿真抓取闭环。

建议阶段：

```text
open_gripper
move_to_pre_grasp
vertical_approach
close_gripper
lift
retreat
place
open_gripper
```

落点建议：

- 抓取候选和策略仍归 `rebotarm_vision`。
- 轨迹生成、验证和执行策略归 `rebotarm_motion`。
- MuJoCo 只作为 `rebotarm_simulation` 的执行和物理反馈层。

### 第七优先级：MuJoCo viewer 可选显示

目标：在不影响 RViz/MoveIt 的情况下，可选打开 MuJoCo 原生 viewer。

操作：

1. 在 launch 中增加 `use_mujoco_viewer` 参数。
2. 处理 GUI 线程和 ROS executor 的关系。
3. 保持默认 headless，避免 CI 或远程环境启动失败。

验收：

- 默认 launch 不弹 viewer。
- 开启参数后可观察 MuJoCo 原生物理界面。

### 第八优先级：真实项目迁移准备

目标：让仿真结果能服务真实机械臂落地。

操作：

1. 建立仿真参数和真实硬件参数对照表。
2. 记录每次调参的 benchmark 输入和结果。
3. 将安全阈值分为仿真阈值和真机阈值。
4. 保证真实硬件执行仍经过 `rebotarmcontroller`。

验收：

- MuJoCo 结果可用于预验证轨迹和抓取策略。
- 真机执行路径没有被仿真 adapter 替代。

## 当前推荐工作顺序

1. 完成关节限制一致性检查。
2. 完成每个关节 step-response 标定。
3. 加强 trajectory tolerance 失败判定。
4. 校准 collision geom。
5. 增加夹爪接触和抓取质量判断。
6. 实现固定物体的自动抓取 demo。
7. 再接入视觉候选和更复杂场景。

## 当前常用命令

轻量化 RViz / MoveIt fake sim：

```bash
ros2 launch rebotarm_bringup rviz_ee_drag_sim.launch.py
```

MuJoCo + MoveIt 物理仿真：

```bash
ros2 launch rebotarm_simulation mujoco_moveit_sim.launch.py \
  use_rviz:=true \
  python_executable:=third_party/rebotarm_mujoco_venv/bin/python
```

查看 MuJoCo 执行误差：

```bash
cat /home/a/project/rebot_Arm/build/mujoco_runs/latest/summary.json
```

夹爪控制：

```bash
ros2 service call /rebotarm/gripper/set \
  rebotarm_msgs/srv/SetGripper \
  "{position: 0.03, max_effort: 1.0}"
```
